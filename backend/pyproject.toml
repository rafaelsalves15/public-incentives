[project]
name = "public-incentives-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "psycopg2-binary",
  "alembic",
  "pydantic",
  "python-dotenv",
  "httpx",
]

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
